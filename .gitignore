.DS_Store
node_modules/

# machine-local Claude Code settings (contains absolute paths)
.claude/settings.local.json

# scheduled-job logs
.build/logs/

# updater-slot lock dirs (self-releasing; stale dirs are reaped by pid check)
.build/locks/
